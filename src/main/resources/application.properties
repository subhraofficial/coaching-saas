spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
app.jwt.secret=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_KEY_32_CHARS_MIN
app.jwt.expiration-ms=604800000
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#app.firebase.credentials=classpath:firebase-service-account.json
server.port=${PORT:8081}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=subhrakamalpayra@gmail.com
spring.mail.password=syio wtvg kipv xkze
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# OTP settings
app.otp.expiry-minutes=5
app.otp.cooldown-seconds=30

# Razorpay
razorpay.key_id=YOUR_KEY_ID
razorpay.key_secret=YOUR_KEY_SECRET
razorpay.webhook_secret=TEMP_SECRET
# Razorpay Plan ID for PRO Monthly (create once in Razorpay Dashboard)
razorpay.pro_plan_id=plan_xxxxxxxxxxxxx
razorpay.pro_monthly_amount_inr=299
# show price on UI
